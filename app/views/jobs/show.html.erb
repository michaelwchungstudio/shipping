<div class="job-show-wrapper">
	<h1>Job: </h1>
	<div><%= @job.name %></div>
	<div><%= @job.origin %></div>
	<div><%= @job.destination %></div>
	<div><%= @job.descrip %></div>
	<div><%= @job.cost %></div>
	<div><%= @job.containers_needed %></div>
	<% b = @job.boats %>
		<% for boat in b %>
			<div><%= boat.name %></div>
		<% end %>
	<div>
		<%= link_to "Edit this job", edit_job_path(@job) %>
		<%= link_to 'Delete this job',  @job,  method: :delete, data: { confirm: 'Are you sure?' } %>


	</div>

</div>

